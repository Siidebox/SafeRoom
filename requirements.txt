# SafeRoom dependencies are declared in pyproject.toml.
#
#   pip install -e ".[viz,ml]"                 radar + visualizer + ML
#   pip install -e ".[dashboard]"              monitoring dashboard
#   pip install -e ".[dev]"                    pytest + ruff
#   pip install -e ".[viz,ml,dashboard,dev]"   everything
#
# This file exists so that `pip install -r requirements.txt` still works.
-e .[viz,ml]
